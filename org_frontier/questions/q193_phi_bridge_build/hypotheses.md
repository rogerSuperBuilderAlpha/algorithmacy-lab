# q193 — hypotheses

Fixed before computing.

**H1.** The per-worker Φ_coord — the exact IIT-4.0 max Φ_MIP over the worker's W-S-C form, with the
system rule parameterized by that worker's reported interdependence (TI), commit-vs-convey authority
(SA), and substitutability (SU) — correlates positively with the latent ACS-total factor score across
the cohort: r > 0.20 with a 95% confidence interval that excludes 0.

Null: the r confidence interval includes 0.

**H2.** On a control cohort whose forms are rewired to be dyadic and factorizable (Φ_coord identically 0
by construction), the Φ_coord-to-ACS correlation vanishes (|r| < 0.10) while it survives in the bridge
cohort. This locates the association in the form's irreducibility, not in a scale shared between Φ_coord
and ACS.

Null: the control r is as large as the bridge r.
