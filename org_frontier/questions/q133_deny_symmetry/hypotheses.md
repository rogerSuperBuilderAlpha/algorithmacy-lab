# Q133 — hypotheses (fixed before computing)

Q132 read value capture across baselines under the approve agenda and found extraction on the XOR baseline,
which re-integrates under approve. Q127 established that under the deny agenda the re-integrating baseline is
XNOR. Q133 runs the deny-agenda value sweep so the mirror reproduces from a script.

- **H1.** Under deny, the re-integrating baseline is XNOR (Φ rises 0.5 → 2.0) and the mediator captures
  two-thirds there — extraction, the mirror of Q132's XOR-under-approve.
- **H2.** AND/OR still destroy under deny; XOR does not re-integrate under deny.
- **Null.** The deny agenda gives the same extracting baseline as approve.

Method: the interested mediator imposes deny on the k least-warranted states, faithful baseline elsewhere;
Shapley value of subsystem Φ at the verdict's integrating state (Q132 convention), four baselines.
