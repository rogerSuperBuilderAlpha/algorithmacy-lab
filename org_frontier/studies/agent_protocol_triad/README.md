# Agent protocol triad (#37)

Two LLM-style agents + protocol node: does design set dyadic/triadic
like the commit, and can negotiation be triadic without a human?

| file | role |
|---|---|
| `hypotheses.md` | H1–H3 fixed before compute |
| `analyze_protocols.py` | exact Φ panel |
| `FINDINGS.md` | PROTOCOL_IS_COMMIT |
| `results/panel.csv` | written by the smoke |

```
python org_frontier/studies/agent_protocol_triad/analyze_protocols.py
```
