# Instrument — ternary pivotality

**Stock pin:** `pyphi @ feature/iit-4.0` rejects ternary SBS `(27,27)`.

**Science path:** vendored overlay `third_party/pyphi_iit4_mv` (M2 exact
IIT-4.0 Φ via `MultivaluedSubsystem` + `sia`). No embeddings. No
`pyphi@nonbinary`. No CI-off `int(log2)` trap.

Path (C) NOT_TESTABLE applied only while M2 was blocked; science now
runs on the overlay (see FINDINGS).
