# Ternary pivotality — hypotheses (fixed after instrument decision C)

**Question (RESEARCH_AGENDA_50_V2 #1).** Do ternary parties
(idle / engaged / overcommitted) change which party is pivotal, or does
the two-condition account survive multivalued state? (parked WAVE10 #5)

**Instrument.** Path **(C)** — see `INSTRUMENT.md`. Exact ternary
IIT-4.0 Φ is **not available** on this pin. Scientific H3–H5 are
**NOT_TESTABLE** if H2 fails.

**Already known (cited, not reopened).**
- Two-condition account (probes #11–#12): bidirectional coupling is
  necessary for core membership; among coupled nodes, influence
  predicts inclusion.
- `shared_mediator_ternary/`: same pin rejects ternary TPMs
  (TOOLING_GAP) — pointer; different question (merge survival).
- PE / AI / formal / stoch / estimation / construct-omit closed.

**Universe (when H2 holds).** Ternary alphabet {0,1,2}; nodes (W,S,C);
commit `S'=min(W,C)`; parties copy S. Exact IIT-4.0 major complex.

**Definitions.**
- **Bidirectional:** node feeds S and reads S (or is read by a node
  that feeds it, in the mediated sense of the binary account).
- **Non-bidirectional / spectator:** update ignores the commit loop
  (sticky self) or is omitted from S’s determination.
- **Two-condition survives:** non-bidirectional nodes out of core;
  bidirectional pivotal parties in (binary control). Ternary survival
  requires the same pattern under exact ternary Φ.

## H1 — binary two-condition control

On designed binary forms: faithful triad has {W,S,C} in core; a
non-bidirectional worker (sticky or omitted from S) is out of the
major complex. Null: control fails (instrument unhealthy).

## H2 — IIT-4.0 accepts ternary n=3 TPM

`Network` builds on a deterministic ternary min-AND triad SBS
(shape 27×27), or exposes `num_states_per_node` usable with
`new_big_phi`. Null: capability absent → H3–H5 **NOT_TESTABLE**.

## H3 — ternary non-bidirectional stays out

If H2: under ternary dynamics, a sticky/omitted worker is absent from
the major complex. Null / NOT_TESTABLE if H2 fails.

## H4 — ternary bidirectional pivotal parties stay in

If H2: ternary min-AND faithful triad keeps W and C in the major
complex. Null / NOT_TESTABLE if H2 fails.

## H5 — graded state changes who is pivotal vs binary

If H2: either core membership or influence-rank pattern differs from
the binary two-condition reading on the matched architecture. Null:
identical account (two-condition survives). NOT_TESTABLE if H2 fails.
