# Omit cycle band grammar at n=8–9 (agenda V4 #8)

Does V3 #8’s band grammar at n=7 still hold at n=8–9, or does a new morph
appear once more cycle types fit?

## Run

```
python org_frontier/studies/omit_cycle_band_n89/analyze_band_n89.py
```

## Result in one line

**SCALE_BLOCKS_EXACT_PHI** — cycle-type catalog expands (10→14→21) but
exact core-Φ at n=8–9 is not adjudicable; n=7 band grammar unrebutted.

## Scope

Lean panel (n=7 control + MC catalogs + optional exact micro). Full
designed census at n=8–9 is intractable under the lab stack.

## Hypotheses

Fixed before computing in [`hypotheses.md`](hypotheses.md).

## Priors

V3 #8 `BAND_GRAMMAR_HOLDS`; V2 #42 `SCALE_MORPHS`. Exact binary IIT-4.0.
