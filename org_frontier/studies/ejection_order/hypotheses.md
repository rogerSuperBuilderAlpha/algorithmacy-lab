# Extractive ejection order — hypotheses (fixed before computing)

**Question (RESEARCH_AGENDA_50_V2 #36).** Does an extractive commit’s
ejection order (#110) predict which real platform stakeholders lose
standing first?

**Already known (cited, not reopened).**
- #110: threshold commit `S=1 iff W+C+w_P·P ≥ 2` — `w_P=0` → `{W,S,C}`;
  `w_P=1` → null (majority-rule factor); `w_P≥2` → extractive `{S,P}`.
  W and C drop together (role symmetry #55); path through null, not
  an all-four core.
- #78: extractive S←P leaves `{S,P}`.
- #55: worker↔counterpart role symmetry.
- #33 SHARP_FULL_CAPTURE / #35 DROP_AT_FIRST_SUBST — pointers.
- #29–#34 — pointers only.
- Other lanes closed.

**Universe.** Exact binary IIT-4.0 Φ. Designed extractive tilt /
ablation panel on `(W,S,C,P)` and `(W,S,C,P,R)`. Candid N. In-silico
Boolean abstractions — evidence about model standing, not measured
firms.

**Definitions (fixed).**
- **#110 path:** cores traverse `{W,S,C}` → null/empty → `{S,P}` as
  owner weight rises; W and C co-absent whenever either is absent at
  max-Φ.
- **Co-ejection:** W and C enter or leave the major complex together
  on a tilt sweep (no W-without-C or C-without-W at the recorded
  rungs).
- **Strict stakeholder order:** a transferable total order among
  {W, C, R, …} (e.g. W before C before R) that holds across the
  designed extractive encodings.
- **Owner endpoint:** major complex `{S,P}` or `{S,P,R}` under heavy
  extractive commit.

## Panel (designed)

| id | role |
|---|---|
| #110 w_P = 0,1,2,3 | reproduce probe path |
| +R observe / gate sweeps | extended stakeholders |
| named extractive ladder | faithful → P-gate → S'=P |
| commit ablations | drop W / C / P from S |
| P∧R / P∨R extractive | owner±regulator endpoints |

## H1 — #110 path reproduces (parties co-eject to owner)

The w_P sweep reproduces `{W,S,C}` → null → `{S,P}` with W/C
co-ejection. Null: path differs (e.g. all-four core, or W before C).

## H2 — strict fine order among W/C/R predicts first loss

Across the extended panel, a single total order (W≺C≺R or similar)
ranks who leaves first under extractive tilt. Null: W/C stay tied and
R’s position flips with encoding (no transferable fine ranking).

## H3 — encoding sets residual standing (R / ablation)

At least two extractive encodings disagree on whether R shares the
owner endpoint or exits, or ablation of W vs C from the commit ejects
only that party — so residual order is encoding-local. Null: every
extended form agrees on one residual ranking beyond #110.
