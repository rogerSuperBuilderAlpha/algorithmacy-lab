# Conjunctive law on real Boolean renders — hypotheses (fixed before computing)

**Question (RESEARCH_AGENDA_50_V2 #45).** Is the conjunctive Φ = n−1 law
(#116) visible in any real coordination dataset rendered as a Boolean
form?

**Instrument.** Exact binary IIT-4.0 Φ (stock pin). Prefer existing
recurrence OSS pipelines; no new scrapes. In-silico on elicited /
fitted renders of committed CSVs.

**Pointers.** #43 PARTIAL_ALIGNMENT; #44 WAGEMAN_PREDICTS_VERDICT;
`CONSTRUCT_VALIDITY_ARC.md`. Synthetic #116 hub is the control, not the
claim.

**Conjunctive signature (law at size n).** Major-complex Φ within
`1e-6` of `n−1` **and** `|core| = n` (full). The n=3 case Φ=2 full core
is the ordinary faithful triad — necessary but **not** evidence of the
*scaling* law by itself.

**Law visible (H1 bar).** Signature at **n≥4** on a real-data render,
**or** signature at **two** distinct sizes from real-data renders
without replacing the institutional wiring by `#116` hub rules.

**Datasets / renders (existing lab pipelines)**

| source | render | modeling strength |
|---|---|---|
| `recurrence/event_series/` (PyPhi PRs) | institutional merge triad (v9) | elicited rule |
| `recurrence/review_heavy/` (sklearn) | four-role review+merge (v10) | elicited rule |
| `recurrence/bot_merged/` (k8s Tide) | Prow approval+bot+code (v11) | elicited rule |
| `recurrence/real_series/` (PyPhi commits) | Boolean fit to weekly activity (v8) | fitted, coarse |
| same role counts as v9/v10 | `#116` `single_hub(n)` forced | **strong** modeling |

**Primary observables.** n; structure; major-complex Φ; \|core\|; Φ−(n−1);
signature yes/no; modeling class.

## H1 — some real Boolean render shows the Φ≈n−1 conjunctive signature

At least one institutional or fitted render meets the H1 bar above.
Null: no such render.

## H2 — null / not visible

No institutional, fitted, **or** strong-hub render shows the signature
at n≥4 (and multi-size real evidence is absent). Null: some render does.

## H3 — visible only after strong modeling choices

Institutional + fitted renders fail the H1 bar, **but** forcing `#116`
hub wiring on the same role counts (n=3 and n=4) recovers the signature
at both sizes. Null: strong hub also fails, or institutional already
passes H1.

## Reading keys

- **LAW_VISIBLE:** H1.
- **LAW_NOT_VISIBLE:** H2.
- **LAW_ONLY_UNDER_STRONG_MODEL:** H3.
