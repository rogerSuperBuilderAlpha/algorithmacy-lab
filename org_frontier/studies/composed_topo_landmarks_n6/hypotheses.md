# composed_topo_landmarks_n6 — hypotheses (fixed before computing)

**Question (RESEARCH_AGENDA_V4 #10).** At n≥6, do V3 #4–#7’s
**composed-topology** verdicts (necklace compose, ring-of-hubs factor,
shared-mediator 2k, hybrid closure) remain discrete landmarks under a
denser random-coupling draw — or do new interstitial atoms dominate?

**Already known (cited, not reopened).**
- V3 #4 `local_triad_necklace` → `COMPOSE_LANDMARK_OR_COLLAPSE` (AND
  necklace = ring Φ=4 full; OR/directed collapse).
- V3 #5 `ring_of_hubs` → `FACTORS_NO_COMBINE` (hubs Φ=6 incomplete).
- V3 #6 `shared_mediator_k` → `SCALE_2K_OR_REFUSES` (AND Φ=2k; OR refuses).
- V3 #7 `hybrid_ff_recurrent_seam` → `CLOSURE_HOLDS_HYBRID` (AND core
  stays recurrent; OR relocates to FF).
- V2 #18 `random_coupling_ensemble` → `DISCRETE_LANDMARKS` at n=4
  (n=6 left out of budget there).

**Gap.** V3 panels pin the four class families at their native sizes.
V4 #10 asks whether a denser n≥6 random-AND draw stays on that discrete
atom set, and whether lean designed densifications / lifts preserve the
class families or invent interstitial Φ.

**Universe.** Binary exact IIT-4.0. Conjunctive AND generators unless
noted. Lean panel: V3 controls (committed) + designed n=6 densify/lifts
+ thin random n=6 (fixed_k∈{2,3}, ER p=0.35; N=28). Exact n≥8 motif
lifts out of lean scope (document if blocked).

## Landmark set L6

L6 = {2, 3, 4, 5, 6, 8, 9, 12, 30} — V2/V3 poles, interiors, and the
composed-family atoms (collapse/factor/compose/2k/hub).

## H1 — V3 #4–#7 controls reproduce

Committed panels still show the four class families (compose Φ=4 full;
factor Φ=6 incomplete; AND Φ=2k full; hybrid AND recurrent locus).
Null: any control drifts.

## H2 — designed n≥6 lifts keep class family

Hybrid n=6 AND keeps recurrent closure; roh densify still factors;
necklace AND still composes (or collapses under densify into a known
atom). Null: a lift invents a new class or interstitial Φ.

## H3 — denser random n=6 stays on L6

≥90% of random samples land on L6. Null: on-landmark rate < 0.9.

## H4 — no interstitial atoms in the lean random window

The set of observed Φ values is ⊆ L6. Null: any off-L6 atom appears.

## Reading keys

- **LANDMARKS_HOLD_NGT6:** H1 ∧ H3 ∧ H4 — discrete composed landmarks
  survive denser n≥6 coupling; no interstitial domination.
- **INTERSTITIAL_DOMINATES:** ¬H3 or ¬H4 — new atoms fill the gaps.
- **CLASS_BREAKS:** H1 fails or a designed lift invents a new class
  outside compose/factor/2k/closure/collapse.
- **CONTROLS_FAIL:** ¬H1.
