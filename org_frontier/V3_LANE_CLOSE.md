# V3 lane close

RESEARCH_AGENDA_V3 #1–#16 are **Answered**. This note records what the
lane settled and what a V4 would open. Exact binary IIT-4.0 Φ; in-silico
throughout. V2 science cells stay closed.

## What #1–#16 settled

| theme | cells | settled claim |
|---|---|---|
| Template / algebra | #1–#3 | Dual-mediator census factors into five; majority collapses to redundancy; mixed seats → blindspot dominates |
| Composed topology | #4–#7 | Necklace compose-or-collapse; ring-of-hubs factors (no hybrid); shared-mediator AND scales as 2k or refuses; hybrid FF+recurrent seams keep closure |
| Discrete omit / atoms | #8–#10 | Band grammar holds at n=7; ring–pool interiors sprout new atoms; parity-hub law holds for n>6 |
| Graded / noise / validation | #11–#14 | Graded min-commit stays sharp across topo; same p* under composed carriers; weakest Boolean render is role-count→hub; Wageman W+n predicts Φ landmarks |
| Estimation residuals | #15–#16 | Ring prior restores hide-party MI (hub does not); **alternating party duty recreates the δ=0 cliff** — losing joint party–party observation is enough |

## Estimation residual in one line

Partial observation fails when **party–party joints** are missing —
total silence on a party (#24/#16 zero-duty) **or** anti-correlated
duty that never co-observes the two parties (#16 alternation). Sparse
independent δ≥0.10 and phase-locked δ=0.5 hold. Imputation can restore
(#15 ring / copy-W); a hub prior alone does not.

## What V4 would open

V3 stays inside designed Boolean forms and cheap MI screens. A V4
should leave that sandbox on purpose:

1. **Transfer of the joint-observation requirement** — does
   `ALTERNATION_RECREATES_CLIFF` survive outside strict-mediation
   `family_n3` (hub / chain / pool / multifamily), and under exact Φ
   labels rather than MI alone?
2. **Field / instrument bridge** — pair the Wageman×landmark result
   (#14) with a real survey+topology packet, or test whether logged
   collaboration graphs show the party-joint duty failure mode.
3. **Beyond-binary / continuous residual** — only if a V3 claim is
   blocked without it (optional M3 subset-Φ fidelity remains tooling,
   not a default first cell).

**Suggested V4 first cell:** multifamily / cross-topo transfer of
`ALTERNATION_RECREATES_CLIFF` under exact Φ (not only MI) — does losing
joint party observation cliff the screen outside strict mediation?

## Reproduce anchors

```
python org_frontier/studies/topology_aware_imputer/analyze_imputer.py
python org_frontier/studies/correlated_party_duty/analyze_duty.py
```
