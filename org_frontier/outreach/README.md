# outreach & upstream contributions

Staged correspondence (drafts the human reviews and sends) and a log of contributions back to the
tools/communities org_frontier builds on. Sending outward correspondence and submitting PRs is the
human's call; this folder records what was prepared and what was actually submitted.

## Upstream contributions

| Project | What | Status |
|---|---|---|
| phyid (`Imperial-MIND-lab/integrated-info-decomp`) | Example notebook `examples/coordination_phiid.ipynb` — ΦID applied to coordination dynamics (imitative→redundancy, leader–follower→transfer, complementary→redundancy-collapse). Demonstrates ΦID on a multi-agent-coordination series. | **PR #6 opened** (from fork `rogerSuperBuilderAlpha:coordination-example`) |
| PyPhi (`wmayner/pyphi`) | Candidate: docs/examples for `new_big_phi.maximal_complex` on small structured systems; issues for the `PYPHI_WELCOME_OFF` env-timing quirk and the `phi_structure` `resolve_congruence` warning. | not yet prepared |

## Correspondence — status

- [`niizato_note.md`](niizato_note.md) — **SENT 2026-06-03** to `t_niizato@yahoo.co.jp` (Takayuki
  Niizato's published corresponding-author address). Shares the Φ-vs-MI/TE replication (probe 45) and
  cites the fish-school work as the exact-Φ-on-collectives precedent.
- **`pyphi-users` group post — SENT 2026-06-03** via Gmail to `pyphi-users@googlegroups.com` (subject
  "Using exact IIT-4.0 Φ as a structural classifier for small mediated systems"). NOTE: group
  membership was still pending at send time, so the message is most likely held in the list's
  **moderation queue** until an owner approves the join request / releases it — i.e. sent but not yet
  necessarily posted publicly.
- Candidates (not yet drafted): Engel & Malone (MIT CCI) re probe 48's Φ⊥performance null;
  Mediano & Rosas re probes 46/47 (O-information, ΦID on coordination); Buldú group re probe 51.

In both sends the auto-inserted phone number was removed from the body before sending.
