# Paper 7 panel reviews (v2 → v3)

Five independent reviews of `post7_korporation.md` v2 (the Sonnet APA rewrite from the wf_aae2608b
grounding run). The v3 rebuild adopts the panel's convergent findings; the sourcing note records the
mapping.

- `hegel_specialist.md` — minor revisions; "most translation-disciplined manuscript in the series."
  Two exegetical fixes decided by verified Knox text: "rightly abolished" mis-states Hegel (he wanted
  the guild opened, not abolished); the welfare functions (§§252–253) undersold. Verdict minor.
- `formal_specialist.md` — accept with revisions; verified every catalog quote and margin. The
  load-bearing fix: the cells paragraph misreported the four cells (guild = contingent, not the
  reducible self-joiner); plus the scope leak (the corporation "passes the causal test" it was never
  run on) and the joint-condition-posited point. Vindicates the honest-scope discipline.
- `reviewer2_hostile.md` — reject-as-stands; the verdict-grammar charge (the body reads verdicts off
  an uncomputed corporation while the scope section admits it was never run — "structurally identical,"
  "strongest single result" vs "the verdict is the case's to give").
- `copyeditor.md` — near-clean; two quotation over-extensions ("because," "would") and a truncated
  escrow block quote; all seven Hegel quotes pass the Knox/Nisbet traps.
- `voice_reader.md` — the cabinetmaker opening is "the best-earned in the series"; the missing temper
  paragraph at the platform (the cabinetmaker→driver arc), the Paper-8 trailer, the "apparatus needs
  restating" boilerplate.

Convergent verdict: the paper's honest-scope discipline holds (formal specialist), but the body let
computed-catalog authority bleed onto the uncomputed Korporation. v3 marks the mappings as readings,
names the card as owed, fixes the cells paragraph, corrects "rightly abolished," concedes the welfare
functions, retitles/splits the platform section, and adds the temper paragraph that closes the arc.
