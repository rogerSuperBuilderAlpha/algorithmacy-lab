"""Φ-structure suite: is scalar Φ an impoverished summary of the cause-effect
structure?

Barrett et al. (2026) argue that a single number Φ should be replaced by a
suite of quantities giving a multi-dimensional characterization. This experiment
extracts the full IIT-4.0 Φ-structure (distinctions + relations + composition)
for many (network, state) pairs and asks what the suite captures that scalar Φ
does not.
"""

import os

os.environ.setdefault("PYPHI_WELCOME_OFF", "true")
